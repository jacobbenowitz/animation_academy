// create new instance of file reader to parse csv into text
// const reader = new FileReader();

export default class Levels {
  constructor() {
    
    // this.levels = this.csvToArray(csvFile);
    
  }


  // csvToArray(csvFile) {
  // }
}